# IMPORTANT: We build a compiled executable (army), so we can exclude most source files
# This dramatically reduces slug size

# Keep only:
# - army (compiled executable)
# - Procfile
# - package.json (for heroku-postbuild)
# - node_modules (needed during build, cleaned up after)

# Exclude everything else:

# Source code (not needed after compilation)
backend/
frontend/
*.ts
*.tsx
*.jsx

# Git and version control
.git/
.gitignore

# Build configuration
.buildpacks
bunfig.toml

# Documentation
*.md
docs/

# Tests
*.test.*
*.spec.*
__tests__/
test/
tests/

# IDE
.vscode/
.idea/

# Environment
.env
.env.*

# TypeScript
tsconfig.json

# Bun
bun.lockb

# CSS source files (compiled into executable)
frontend/src/index.css
